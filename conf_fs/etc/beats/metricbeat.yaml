path.home: /var/beats/metricbeat

metricbeat.modules:
- module: system
  metricsets:
    - cpu
    - load
    - memory
    - network
    - filesystem
  enabled: true
  period: 10s
  cpu.metrics: ["percentages"]
